<Page x:Class="DemoTranning.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DemoTranning.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <Grid>
        <StackPanel Orientation="Vertical" Height="400" Width="400">
            <Label Content="Логин"/>
            <TextBox x:Name="LoginTextBox"/>
            <Label Content="Пароль"/>
            <PasswordBox x:Name="PassowrdTextBox"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="Вход" x:Name="LoginButton" Click="LoginButton_Click"/>
                <Button Content="Регистрация" x:Name="registerButton" Click="registerButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
